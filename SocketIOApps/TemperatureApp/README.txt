We will build a Real-Time Server that will show the current
temperature in Florence and will update every 10 seconds.

The server will use Sicket.IO to emit a message every 10 
seconds and the client will listen for the same message over
a real-time socket.